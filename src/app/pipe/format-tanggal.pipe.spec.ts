import { FormatTanggalPipe } from './format-tanggal.pipe';

describe('FormatTanggalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTanggalPipe();
    expect(pipe).toBeTruthy();
  });
});
