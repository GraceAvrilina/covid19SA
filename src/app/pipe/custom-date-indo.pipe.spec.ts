import { CustomDateIndoPipe } from './custom-date-indo.pipe';

describe('CustomDateIndoPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateIndoPipe();
    expect(pipe).toBeTruthy();
  });
});
