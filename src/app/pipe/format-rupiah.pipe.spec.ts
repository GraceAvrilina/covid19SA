import { FormatRupiahPipe } from './format-rupiah.pipe';

describe('FormatRupiahPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRupiahPipe();
    expect(pipe).toBeTruthy();
  });
});
